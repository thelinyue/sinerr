---
title: AUR (Advanced)
description: Install Sinerr using the Arch User Repository
sidebar_position: 1
---

# AUR
:::warning
Third-party installation methods are maintained by the community. The Sinerr team is not responsible for these packages.
:::

:::warning
This method is not recommended for most users. It is intended for advanced users who are using Arch Linux or an Arch-based distribution.
:::

## Installation

To install Sinerr from the AUR, you can use an AUR helper like `yay` or `paru`:

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs groupId="aur-methods" queryString>
  <TabItem value="yay" label="yay">
    ```bash
    yay -S sinerr
    ```
  </TabItem>
  <TabItem value="paru" label="paru">
    ```bash
    paru -S sinerr
    ```
  </TabItem>
</Tabs>

:::info
After installing Sinerr, configure it by visiting the web UI at `http://[address]:5055` and completing the setup steps.
:::

:::tip
You can find the environment file at `/etc/conf.d/sinerr` and the service file at `/etc/systemd/system/sinerr.service`.
:::
